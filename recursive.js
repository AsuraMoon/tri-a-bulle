function factoriel(nbr) {
    if (nbr > 2){
        return nbr * factoriel(nbr-1)
    }
    else {
        return 1
    }
}

chiffre=Number(prompt("Quel est le chiffre à vectoriser : "))
console.log(factoriel(chiffre))

function retrouve(nombre) {
    if (nombre<value){
        return facvalue/2
    }
}